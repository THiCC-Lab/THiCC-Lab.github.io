---
layout: page
---
<div class="post">
  <article>
    {% if page.profiles %}
      <h2 class="category">Director of THiCC Lab</h2>
      {% for profile in page.profiles %}
        {% if profile.role == "pi" %}
        <hr>
        <div class="profile float-{% if profile.align == 'left' %}left{% else %}right{% endif %}">
          {% if profile.image %}
            {% assign profile_image_path = profile.image | prepend: 'assets/img/' %}
            {% if profile.image_circular %}
              {% assign profile_image_class = 'img-fluid z-depth-1 rounded-circle' %}
            {% else %}
              {% assign profile_image_class = 'img-fluid z-depth-1 rounded' %}
            {% endif %}
            {% capture sizes %}(min-width: {{ site.max_width }}) {{ site.max_width | minus: 30 | times: 0.3}}px, (min-width: 576px) 30vw, 95vw"{% endcapture %}
            {% include figure.liquid loading="eager" path=profile_image_path class=profile_image_class sizes=sizes alt=profile.image %}
          {% endif %}
          {% if profile.more_info %}
            <div class="more-info" align="center>{{ profile.more_info }}</div>
          {% endif %}
        </div>

        <div class="clearfix">
          {% if profile.content %}
            {% capture profile_content %}{% include_relative {{ profile.content }} %}{% endcapture %}
            {{ profile_content | markdownify }}
          {% else %}
            {{ content }}
          {% endif %}
        </div>
        {% endif %}
      {% endfor %}
      <h2 class="category">Graduate Students</h2>
      {% for profile in page.profiles %}
        {% if profile.role == "grad" %}
        <hr>
        <div class="profile float-{% if profile.align == 'left' %}left{% else %}right{% endif %}">
          {% if profile.image %}
            {% assign profile_image_path = profile.image | prepend: 'assets/img/' %}
            {% if profile.image_circular %}
              {% assign profile_image_class = 'img-fluid z-depth-1 rounded-circle' %}
            {% else %}
              {% assign profile_image_class = 'img-fluid z-depth-1 rounded' %}
            {% endif %}
            {% capture sizes %}(min-width: {{ site.max_width | times: 0.5 }}) {{ site.max_width | minus: 30 | times: 0.3}}px, (min-width: 576px) 30vw, 95vw"{% endcapture %}
            {% include figure.liquid loading="eager" path=profile_image_path class=profile_image_class sizes=sizes alt=profile.image %}
          {% endif %}
          {% if profile.more_info %}
            {{ profile.more_info }}
          {% endif %}
        </div>

        <div class="clearfix">
          {% if profile.content %}
            {% capture profile_content %}{% include_relative {{ profile.content }} %}{% endcapture %}
            {{ profile_content | markdownify }}
          {% else %}
            {{ content }}
          {% endif %}
        </div>
        {% endif %}
      {% endfor %}
    <div>
      <h3>Lab Alumni</h3>
      Haya Alshayji (Current PhD IME)<br />
      Julian Gay (CSE MS graduate)<br />
      Girishwar Srinivasan (CSE MS graduate)<br />
      Naomi Karnuah (PSU IME BS graduate)<br />
      Josiah Harrison (PSU DS BS graduate)<br />
      Roger Carter  (PSU CS BS graduate)<br />
      Jonathan Ntow (PSU CS BS graduate)<br />
      Dixon Zor (PSU CS BS graduate)<br />
      Anhjay Brown (PSU DS BS graduate)<br />
      Kai Holden (PSU IST BS graduate)<br />
      Sage Phillips (PSU IST BS graduate)<br />
      Sharon Liu (PSU CS BS graduate)<br />
      Allan Julian (PSU DS BS graduate)<br />
      Kobe George (PSU CS BS graduate)<br />
      Emi Erekosima (PSU IST BS graduate)<br />
      Shan Wang (Informatics PhD graduate)<br />
      Dipal Patel (PSU CS BS graduate)<br />
      Eryn Dennis (PSU IST BS graduate)<br />
      Virginia Nyaribari (PSU IME BS graduate)<br />
      Mysha Rahman (PSU IST BS graduate)<br />
      Jules Ward (Bucknell CS BS graduate)<br />
      Akil Atkins (Bucknell CS BS graduate)<br />
    </div>
    {% endif %}
  </article>
</div>
